export type LayoutValues = {
    marginLeft: string;
    titleFontSize: string;
    subtitleFontSize: string;
    infoFontSize: string;
};

export type ResponsiveLayout = {
    small: LayoutValues;
    medium: LayoutValues;
    large: LayoutValues;
};
